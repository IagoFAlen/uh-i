import { styled } from "..";

export const TagsContainer = styled('div', {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    gap: '$4',
    width: '100%',
    paddingBottom: '$4',
})